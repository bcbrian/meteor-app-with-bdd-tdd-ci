
import { FlowRouter } from 'meteor/kadira:flow-router';

FlowRouter.route('/', {
  name: 'hack route for tests',
  action() {},
});
FlowRouter.route('/local', {
  name: 'hack route for tests',
  action() {},
});
